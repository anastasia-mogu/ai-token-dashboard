name: 公开仓安全检查
on:
  push:
  pull_request:
jobs:
  safety:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      - name: 检查禁入文件和敏感特征
        run: python3 scripts/check-public-safety.py
